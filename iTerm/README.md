# Tetrachroma for iTerm

+ Navigate to `Preferences` > `Profiles` > `Colors`

+ In the bottom-right, select the `Color Presets` dropdown.

+ Use the `Import...` button, select `Tetrachroma.itermcolors` and confirm.

+ Re-open the `Color Presets` dropdown to find Tetrachroma in the list.